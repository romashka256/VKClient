<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/dialog_item_dialog_photo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@mipmap/ic_launcher" />

    <TextView
        android:layout_toRightOf="@id/dialog_item_dialog_photo"
        android:layout_marginLeft="12dp"
        android:id="@+id/dialog_item_name_dialog"
        android:layout_width="300dp"
        android:textSize="15sp"
        android:layout_height="20dp"
        android:maxLines="1"
        android:text="TextView" />

    <TextView
        android:layout_below="@id/dialog_item_name_dialog"
        android:layout_toRightOf="@id/dialog_item_dialog_photo"
        android:id="@+id/dilalog_item_message_from_dialog"
        android:layout_marginLeft="12dp"
        android:layout_width="300dp"
        android:layout_marginTop="4dp"
        android:layout_height="20dp"
        android:textSize="12sp"
        android:maxLines="1"
        android:text="TextView" />
</RelativeLayout>